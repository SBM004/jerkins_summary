import React from 'react';

export default function BiBuild(props){
    if(props.verification.includes("Binary") && props.verification.includes("Image")){
       if(!props.binaryBroken){
        data=props.failureCount.split(",");;
        
       } 
    }
    return(f);
}